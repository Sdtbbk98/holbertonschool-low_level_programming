#include "main.h"

/**
 * _strlen - function that calculate length
 * @s: , char pointer
 * Return: int, length of @*s
 */

int _strlen(char *s)
{
	int length;
	int index = 0;

	for (; s[index]; index++)
	{
		length = index + 1;
		/**return (length);*/
	}
	return (length);
}
