#include "main.h"

/**
 * _abs -it calculate the absolute value of an integer
 *
 */

int _abs(int num)
{

	if (num < 0)
	{
		num = num * (-1);
		return (num);
	}
	else if (num == 0)
	{
		return (num);
	}
	else
	{
		return (num);
	}
}

